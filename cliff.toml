# git-cliff configuration for Conventional Emoji Commits
# See https://git-cliff.org/docs/configuration

[changelog]
# Changelog header
header = """
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

"""

# Template for the changelog body
# https://keats.github.io/tera/docs/#introduction
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits %}
        - {% if commit.scope %}**{{ commit.scope }}**: {% endif %}\
            {{ commit.message | upper_first | trim }}\
            {% if commit.github.pr_number %} ([#{{ commit.github.pr_number }}]({{ commit.github.pr_url }})){% endif %}\
    {% endfor %}
{% endfor %}\n
"""

# Template for the changelog footer
footer = """
<!-- generated by git-cliff -->
"""

# Remove the leading and trailing whitespace from the templates
trim = true

[git]
# Parse the commits based on https://www.conventionalcommits.org
conventional_commits = true

# Filter out commits that are not conventional
filter_unconventional = false

# Process each line of a commit as an individual commit
split_commits = false

# Regex for parsing and grouping commits
commit_parsers = [
    # Breaking changes (MAJOR)
    { message = "^.*!:", group = "<!-- 0 -->⚠ BREAKING CHANGES" },
    { body = ".*BREAKING[- ]CHANGE.*", group = "<!-- 0 -->⚠ BREAKING CHANGES" },
    { message = "^🚨", group = "<!-- 0 -->⚠ BREAKING CHANGES" },
    { message = "^💥", group = "<!-- 0 -->⚠ BREAKING CHANGES" },

    # Features (MINOR)
    { message = "^✨", group = "<!-- 1 -->✨ Features" },
    { message = "^feat", group = "<!-- 1 -->✨ Features" },
    { message = "^feature", group = "<!-- 1 -->✨ Features" },

    # Bug fixes (PATCH)
    { message = "^🩹", group = "<!-- 2 -->🩹 Bug Fixes" },
    { message = "^fix", group = "<!-- 2 -->🩹 Bug Fixes" },
    { message = "^bugfix", group = "<!-- 2 -->🩹 Bug Fixes" },

    # Performance (PATCH)
    { message = "^⚡", group = "<!-- 3 -->⚡ Performance" },
    { message = "^perf", group = "<!-- 3 -->⚡ Performance" },

    # Refactoring
    { message = "^♻️", group = "<!-- 4 -->♻️ Refactoring" },
    { message = "^refactor", group = "<!-- 4 -->♻️ Refactoring" },

    # Documentation
    { message = "^📚", group = "<!-- 5 -->📚 Documentation" },
    { message = "^docs", group = "<!-- 5 -->📚 Documentation" },

    # Styling
    { message = "^🎨", group = "<!-- 6 -->🎨 Styling" },
    { message = "^style", group = "<!-- 6 -->🎨 Styling" },

    # Testing
    { message = "^🧪", group = "<!-- 7 -->🧪 Testing" },
    { message = "^test", group = "<!-- 7 -->🧪 Testing" },

    # Build system
    { message = "^🔧", group = "<!-- 8 -->🔧 Build System" },
    { message = "^build", group = "<!-- 8 -->🔧 Build System" },

    # CI/CD
    { message = "^🤖", group = "<!-- 9 -->🤖 CI/CD" },
    { message = "^ci", group = "<!-- 9 -->🤖 CI/CD" },

    # Chores
    { message = "^📦", group = "<!-- 10 -->📦 Miscellaneous" },
    { message = "^chore", group = "<!-- 10 -->📦 Miscellaneous" },

    # Reverts
    { message = "^🔄", group = "<!-- 11 -->🔄 Reverts" },
    { message = "^revert", group = "<!-- 11 -->🔄 Reverts" },
]

# Protect breaking changes from being skipped due to matching a skipping commit_parser
protect_breaking_commits = true

# Filter out commits that match these patterns
filter_commits = false

# Regex for matching git tags
tag_pattern = "v[0-9].*"

# Sort the tags topologically
topo_order = false

# Sort the commits inside sections by oldest/newest order
sort_commits = "oldest"

# Limit the number of commits included in the changelog
# limit_commits = 42

[remote.github]
owner = "codekiln"
repo = "langstar"
